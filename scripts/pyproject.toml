[project]
name = "danskprep-scripts"
version = "0.1.0"
description = "Data scraping and seeding scripts for DanskPrep"
requires-python = ">=3.12"
dependencies = [
    "playwright>=1.40.0",
    "supabase>=2.0.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.4",
]

